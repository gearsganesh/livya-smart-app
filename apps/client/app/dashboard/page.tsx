import { createSupabaseServerClient } from '@/lib/supabase-server';
export default async function Dashboard(){const supabase=await createSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();const {data:profile}=user?await supabase.from('profiles').select('full_name').eq('id',user.id).maybeSingle():{data:null};const name=profile?.full_name||user?.email?.split('@')[0]||'there';return <main className="shell"><div className="container"><div className="topbar"><div><span className="eyebrow">LIVYA DASHBOARD</span><h1>Hello, {name}</h1><p className="lead">Your health overview, securely connected to your LIVYA account.</p></div></div><section className="grid"><article className="card"><h2>Health score</h2><div className="metric">--</div><p>Connect health data to calculate your score.</p></article><article className="card"><h2>Latest vitals</h2><p>No measurements available yet.</p></article><article className="card"><h2>Reports</h2><p>Your medical documents will appear here.</p></article></section></div></main>}
